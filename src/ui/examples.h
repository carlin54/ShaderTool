#ifndef EXAMPLES_H
#define EXAMPLES_H

#include <QByteArray>

namespace Examples {
// Built-in sample projects (JSON, same schema as File → Save).
QByteArray jsonBasicsHello();
QByteArray jsonBasicsWarm();
QByteArray jsonBasicsMeshObj();
QByteArray jsonRtMinimalCompileTest();
} // namespace Examples

#endif
